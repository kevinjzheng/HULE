{
  "name": "hule-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx index.ts",
    "build": "tsc -P tsconfig.json && node -e \"const fs=require('fs');fs.mkdirSync('../dist/server',{recursive:true});fs.writeFileSync('../dist/server/package.json',JSON.stringify({type:'commonjs'}))\"",
    "start": "node index.js"
  },
  "dependencies": {
    "msedge-tts": "^2.0.4",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
